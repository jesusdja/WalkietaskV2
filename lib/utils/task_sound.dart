import 'package:flutter/material.dart';

class SoundTask extends StatelessWidget {

  bool status;

  SoundTask({
    this.status = false,
  });

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
